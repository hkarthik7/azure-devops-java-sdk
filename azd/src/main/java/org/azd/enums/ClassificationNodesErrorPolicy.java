package org.azd.enums;

/**
 * Flag to handle errors in getting some nodes. Possible options are Fail and Omit.
 */
public enum ClassificationNodesErrorPolicy {
    /**
     * Fail
     */
    FAIL,
    /**
     * Omit
     */
    OMIT
}
